package map;

import generics.Car;
import products.Product;

import java.util.*;

public class Main {
    public static void main(String[] args) {
        Map<String, String> wordMap = new HashMap<>();
        Map<Car, String> carMap = new LinkedHashMap<>();
        Map<Integer, Product> productMap = new TreeMap<>();
        Map<Integer, List<Product>> listProductMap = new TreeMap<>();

        List<List<Set<String>>> weird = new ArrayList<>();
        List<Set<String>> weird2 = new ArrayList<>();

        weird.add(weird2);



    }
}
